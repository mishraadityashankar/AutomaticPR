#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../create-pull-request/create-pull-request.js" "$@"
  ret=$?
else 
  node  "$basedir/../create-pull-request/create-pull-request.js" "$@"
  ret=$?
fi
exit $ret
